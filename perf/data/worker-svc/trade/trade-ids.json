{
  "_comment": "Trade ID pool shared by the detail and risk-metrics scenarios. IDs go stale with the environment; re-capture when switching environments (see README). Keep the placeholder in the repo — the setup-phase preflight intercepts the placeholder and aborts (PREFLIGHT FAILED).",
  "ids": ["TBC-REPLACE-WITH-REAL-TRADE-ID"]
}
