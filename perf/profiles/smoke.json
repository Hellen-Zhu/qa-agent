{
  "name": "smoke",
  "description": "Are the script, data, and pipeline working right now. Mandatory in the same session before any big round — this doubles as the real verification that the static data is still valid.",
  "scenario": {
    "executor": "constant-arrival-rate",
    "rate": 2, "timeUnit": "1s", "duration": "1m",
    "preAllocatedVUs": 5, "maxVUs": 10
  },
  "thresholds": {
    "perf_business_success": ["rate>0.99"]
  }
}
