# 压测产物 —— 每次运行都会生成，不进版本库
results/*
reports/*
!results/.gitkeep
!reports/.gitkeep

# JMeter 运行时垃圾
jmeter.log
*.jtl

# .dat 样本可能很大（large/ 下单个文件可达几十 MB）。
# 目前先全部允许提交，等拿到真实文件、确认体积后再决定是否排除 large/。
# 届时取消下面两行的注释：
# data/dat/large/*
# !data/dat/large/.gitkeep
