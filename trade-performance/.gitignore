# 压测产物 —— 每次运行都会生成，不进版本库（results/ 有自己的 .gitignore）

# 采集来的真实 curl / 响应样本（校准依据，不是可执行脚本）
# ⚠ 必须排除：DevTools 导出的 curl 带着会话 cookie、真实 counterparty 名称，
#   以及有时是内网地址。它的价值是"三个月后能回答当初凭什么这么断言"，
#   放本地即可，不该进版本库。
data/**/_samples/

# .dat 样本可能很大（单个文件可达几十 MB）。
# 目前先全部允许提交，等拿到真实文件、确认体积后再决定是否排除。
# 届时按 productType 目录加排除行，例如：
# data/dat/products/<大文件目录>/*.dat
