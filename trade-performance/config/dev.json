{
  "_comment": "Dimension two: environment. Only describes 'which environment to hit', never contains VU counts/durations (that is the job of profiles/)",

  "services": {
    "_comment": "The 5 services are addressed independently. Deliberately no global host -- a request that forgets to specify its service fails outright instead of silently hitting another service.",

    "workers":       { "_status": "Confirmed 2026-07-28 via real curl",
                       "protocol": "http", "host": "10.198.25.56", "port": 9089, "basePath": "/api/v1" },

    "_placeholder":  "The four groups below are still placeholders (NFR pending confirmation #12). Deliberately not filled in alongside workers -- a correct guess would go unverified, and a wrong guess shows up as 404, which reads as 'endpoint does not exist' rather than 'address misconfigured'. localhost gives connection refused, instantly recognizable as unconfigured. p02 is unaffected.",
    "uc":            { "protocol": "http", "host": "localhost", "port": 8080, "basePath": "/api/v1" },
    "refdata":       { "protocol": "http", "host": "localhost", "port": 8080, "basePath": "/api/v1" },
    "notifications": { "protocol": "http", "host": "localhost", "port": 8080, "basePath": "/api/v1" },
    "ops":           { "protocol": "http", "host": "localhost", "port": 8080, "basePath": "/api/v1" }
  },

  "identity": {
    "_comment": "This project has no login / no token; authorization is entirely determined by X-User-Id. Roles are fixed, never rotated: the four-eyes principle requires checker != maker (NFR SEC-02).",
    "makerUserId":   "maker@sc.com",
    "checkerUserId": "checker@sc.com"
  },

  "timeouts": { "request": "60s" },


  "_grafanaDashboard": "Read by the runners (run.sh / run.ps1), not by the k6 scripts: dashboard URL including any var-host parameters. The runner appends this run's epoch window as &from=/&to= plus &var-testid=<runId> (the run selector used by k6 dashboard 19665 and grafana/oreo-k6-verdicts.json) and prints a click-through link; empty means it prints the raw epoch window instead. GRAFANA_DASHBOARD_URL overrides this for a single run, so CI and one-off runs never need a file edit.",
  "grafanaDashboard": ""
}
