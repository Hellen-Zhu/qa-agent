{
  "name": "smoke",
  "description": "Script self-check, not a performance test. 1 VU running 1 iteration, verifying only: paths reachable, verdicts correct, data loaded, extractors got values.",
  "scenario": {
    "executor": "per-vu-iterations",
    "vus": 1,
    "iterations": 1,
    "maxDuration": "2m"
  },
  "thresholds": {
    "_comment": "No category of error should occur during the smoke stage",
    "oreo_err_technical": ["count==0"],
    "oreo_err_business":  ["count==0"],
    "oreo_ok":            ["count>0"]
  }
}
