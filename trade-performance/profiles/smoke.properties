# ─────────────────────────────────────────────────────────────
# 维度三：负载模型
# 只描述"施加多大压力"，绝不包含 host / 身份 / 数据（那是 config/ 的职责）
# 用法：jmeter ... -q config/dev.properties -q profiles/smoke.properties
#      后指定的 -q 覆盖先指定的
# ─────────────────────────────────────────────────────────────
#
# smoke —— 脚本自检，不是性能测试
# 1 线程跑 1 轮，只验证：路径通、断言对、变量都解析了、提取器拿到值

threads=1
rampUp=1
duration=0
loops=1
scheduler=false

thinkTimeBase=0
thinkTimeRange=0
